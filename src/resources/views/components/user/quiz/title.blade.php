@props(['title' => 'クイズ'])

<section class="p-hero p-quiz-hero">
    <div class="l-container">
        <h1 class="p-hero__title">
            <span class="p-hero__title__label">POSSE課題</span>
            <span class="p-hero__title__inline">{{ $title }}</span>
        </h1>
    </div>
</section>
<!-- /.p-hero .p-quiz-hero -->